kind = "web"
previewPath = "/"
title = "CloudHire"
version = "1.0.0"
id = "artifacts/cloudhire"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24569

[services.development]
run = "pnpm --filter @workspace/cloudhire run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/cloudhire", "run", "build" ]
publicDir = "artifacts/cloudhire/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24569"
BASE_PATH = "/"
